[project]
name = "gui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nicegui>=3.14.0",
    "python-api"
]

[tool.uv]
package = true

[tool.setuptools]
package-dir = {"" = "."}

[tool.uv.sources]
python-api = { workspace = true }
